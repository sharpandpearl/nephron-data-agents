/**
 * Shared utilities for Nephron data scrapers.
 *
 * This package will contain common code used across all scraper modules:
 * - Browser management (Playwright setup)
 * - Cloud storage (R2 upload)
 * - CSV handling
 * - Base product models
 */
package com.nephron.shared;
